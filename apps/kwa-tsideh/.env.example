# No key is required for the six default adapters.
# Optional: raises the GitHub Search rate limit from 10 to 30 req/min.
# NEVER put a secret here that must stay secret -- anything EXPO_PUBLIC_* ships in the bundle.
# For keyed sources, set requiresProxy: true on the adapter and route through the proxy below.
EXPO_PUBLIC_PROXY_URL=
